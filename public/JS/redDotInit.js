// redDotInit.js

// Define the function to start the red dot animation after the black hole animation
export function startRedDotAfterAnimation(redDot) {
    // Assuming some logic to start the animation of the red dot after the black hole animation
    // Example code to animate the red dot:
    setTimeout(() => {
        redDot.classList.add('active');  // You can replace this with your actual animation logic
    }, 1000); // Delay to simulate waiting for the animation to finish
}
